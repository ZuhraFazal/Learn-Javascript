const MyId = 1;
let Name = 'Zuhra';
var age = 21;
city = 'karachi';
let state;

// Cannot change the value of const variable
console.log(MyId);

/*
prefer not to user var due to scope and funtion block
js allow us to create var without a keyword but it is not preferrable
we can declare a variable with out a value
*/

console.table([MyId,Name,age,city,state]);